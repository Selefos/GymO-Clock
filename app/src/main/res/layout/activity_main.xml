<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        android:id="@+id/app_bar_main"
        layout="@layout/app_bar_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/custom_transparent"
        android:fitsSystemWindows="true"
        android:longClickable="true"
        app:headerLayout="@layout/nav_header_main">
        <!--        app:menu="@menu/activity_main_drawer" >-->

        <ExpandableListView
            android:id="@+id/expandable_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:layout_marginTop="@dimen/nav_header_height"
            android:background="@color/custom_transparent"
            android:childDivider="@drawable/divider_list_child"
            android:childIndicator="@color/custom_text_color"
            android:childIndicatorStart="1dp"
            android:divider="@drawable/divider_list"
            android:dividerHeight="1dp"
            android:fitsSystemWindows="true"
            android:groupIndicator="@null"
            android:indicatorLeft="?android:expandableListPreferredChildIndicatorLeft"
            android:longClickable="true" />
    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>